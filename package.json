{
  "name": "wasm-libvirt-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test packages/libvirt-adapter/test/*.test.mjs",
    "check": "npm run build && npm test",
    "release:check": "node scripts/release-check.mjs",
    "release:dry-run": "npm run release:check && npm publish ./packages/libvirt-adapter --access public --dry-run",
    "release:publish": "npm run release:check && npm publish ./packages/libvirt-adapter --access public",
    "prototype:feasibility": "node prototypes/virt-wasm-feasibility/runner.mjs --all",
    "prototype:feasibility:interactive": "node prototypes/virt-wasm-feasibility/runner.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.3"
  }
}
